<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Giaodien">

        <!-- NEW MVC STRUCTURE - All activities in store.view package -->
        <activity
            android:name=".store.view.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".store.view.RegisterStep1Activity" android:exported="false" />
        <activity android:name=".store.view.RegisterStep2Activity" android:exported="false" />
        <activity android:name=".store.view.HomeActivity" android:exported="false" />

        <!-- Profile -->
        <activity android:name=".store.view.ProfileActivity" android:exported="false" />

        <!-- Notification -->
        <activity android:name=".store.view.NotificationActivity" android:exported="false" />

        <!-- Dish Management -->
        <activity android:name=".store.view.DishListActivity" android:exported="false" />
        <activity android:name=".store.view.AddDishActivity" android:exported="false" />
        <activity android:name=".store.view.EditDishActivity" android:exported="false" />

        <!-- Order Management -->
        <activity android:name=".store.view.NewOrdersActivity" android:exported="false" />
        <activity android:name=".store.view.ConfirmedOrdersActivity" android:exported="false" />
        <activity android:name=".store.view.OrdersManagerActivity" android:exported="false" />

        <!-- Promotion Management -->
        <activity android:name=".store.view.PromotionListActivity" android:exported="false" />
        <activity android:name=".store.view.AddPromotionActivity" android:exported="false" />
        <activity android:name=".store.view.EditPromotionActivity" android:exported="false" />

        <!-- Reports & Reviews -->
        <activity android:name=".store.view.SalesReportActivity" android:exported="false" />
        <activity android:name=".store.view.ReviewListActivity" android:exported="false" />
        <activity android:name=".store.view.ReviewDetailActivity" android:exported="false" />

    </application>

</manifest>