<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Giaodien">

        <!-- NEW MVC STRUCTURE - All activities in view package -->
        <activity
            android:name=".view.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".view.RegisterStep1Activity" android:exported="false" />
        <activity android:name=".view.RegisterStep2Activity" android:exported="false" />
        <activity android:name=".view.HomeActivity" android:exported="false" />

        <!-- Dish Management -->
        <activity android:name=".view.DishListActivity" android:exported="false" />
        <activity android:name=".view.AddDishActivity" android:exported="false" />
        <activity android:name=".view.EditDishActivity" android:exported="false" />

        <!-- Order Management -->
        <activity android:name=".view.NewOrdersActivity" android:exported="false" />
        <activity android:name=".view.ConfirmedOrdersActivity" android:exported="false" />
        <activity android:name=".view.OrdersManagerActivity" android:exported="false" />

        <!-- Promotion Management -->
        <activity android:name=".view.PromotionListActivity" android:exported="false" />
        <activity android:name=".view.AddPromotionActivity" android:exported="false" />
        <activity android:name=".view.EditPromotionActivity" android:exported="false" />

        <!-- Reports & Reviews -->
        <activity android:name=".view.SalesReportActivity" android:exported="false" />
        <activity android:name=".view.ReviewListActivity" android:exported="false" />
        <activity android:name=".view.ReviewDetailActivity" android:exported="false" />

    </application>

</manifest>